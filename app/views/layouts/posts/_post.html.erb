<!-- app/views/posts/_post.html.erb -->
<div id="<%= dom_id(post) %>">
  <p>
    <strong>Title:</strong> 
    <%= post.title %>
  </p>
  <p>
    <strong>Content:</strong> 
    <%= post.content %>
  </p>
</div>